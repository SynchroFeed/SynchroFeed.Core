<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <ProjectGuid>{F5BCF341-75E3-4440-932E-1F263D409B6C}</ProjectGuid>
    <TargetFramework>net50</TargetFramework>
    <AssemblyTitle>SynchroFeed.Command.VersioningCheck</AssemblyTitle>
    <Product>SynchroFeed.Command.VersioningCheck</Product>
    <Copyright>Copyright(c) 2018 SynchroFeed</Copyright>
  </PropertyGroup>
  <PropertyGroup>
    <OutputPath>$(SolutionDir)nuget\$(Configuration)\</OutputPath>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Description>SynchroFeed is an extensible framework for integrating with a Nuget-like feeds to perform syncing, cataloging and validation of Nuget and Chocolatey packages. This is an addon for validating packages to make sure they have the same version applied to all dlls within the package.</Description>
    <Version>0.0.0.0</Version>
    <Authors>SynchroFeed</Authors>
    <Company>SynchroFeed</Company>
    <RepositoryUrl>https://www.nuget.org/packages/SynchroFeed.Command.VersioningCheck/</RepositoryUrl>
    <PackageProjectUrl>https://github.com/SynchroFeed/SynchroFeed.Core</PackageProjectUrl>
    <RepositoryType>nuget</RepositoryType>
    <PackageTags>nuget chocolately sync feed package version addon command</PackageTags>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageIcon>SynchroFeed.png</PackageIcon>
  </PropertyGroup>
  <ItemGroup>
    <None Include="..\..\LICENSE">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
    <None Include="..\..\SynchroFeed.png">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
    <None Include="README.md" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\SynchroFeed.Library\SynchroFeed.Library.csproj" />
  </ItemGroup>
  <ItemGroup>
    <Folder Include="Properties\" />
  </ItemGroup>
</Project>